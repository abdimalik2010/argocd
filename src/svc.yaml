apiVersion: v1
kind: Service
metadata:
  labels:
    app: webapp
  name: webapp-svc
  namespace: argocd-resources
spec:
  ports:
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: webapp
  type: LoadBalancer
